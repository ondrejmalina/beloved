# beloved
A simple CLI tool for tracking favorite (or 'beloved') filesystem paths

**This project has ended. I didn't realize that a child process cannot change the working directory of its parent process.**
